default: &default
  adapter: postgresql
  encoding: unicode
  pool: 5
  timeout: 5000
  # Docker Compose adds in the app machine /etc/hosts and entry
  # that points to the container IP where "db" is running
  #@see in docker-compose.yml under "app"
  #     links:
  #       - db
  #
  # It is that IP they put in <link_name>_PORT_5432_TCP_ADDR
  host: <%= ENV['DB_PORT_5432_TCP_ADDR'] || 'localhost' %>
  port: <%= ENV['DB_5432_TCP_PORT'] || '5432' %>

development:
  adapter: postgresql
  encoding: unicode
  database: vetsgovdemo-dev
  pool: 5
  username: vetsgovdev
  password: vetsgovdev

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
#test:
#  <<: *default
#  database: vetsgovdemo-qa
#  username: vetsgovqa
#  password:

#production:
#  <<: *default
#  database: vetsgovdemo-prod
